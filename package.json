{
  "name": "roamsnotes-hugo-pseo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate_pages.js",
    "quality": "node scripts/quality_gate.js",
    "build": "npm run quality && npm run generate && hugo --minify",
    "dev": "npm run generate && hugo server --bind 127.0.0.1 --port 1313 --disableFastRender"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
