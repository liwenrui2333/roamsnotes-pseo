{
  "name": "roamsnotes-hugo-pseo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate_pages.js",
    "images": "node content-engine/gen_images.mjs",
    "quality": "node scripts/quality_gate.js",
    "build": "npm run quality && npm run generate && hugo --minify --baseURL https://www.roamsnotes.com/ && npm run release-gate",
    "release-gate": "node scripts/release_gate.js",
    "dev": "npm run generate && hugo server --bind 127.0.0.1 --port 1313 --disableFastRender"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
